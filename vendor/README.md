# Offline copy of SheetJS CE 0.20.3 (patched).
# Runtime dep uses the CDN URL in package.json / overrides:
#   https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz
# Re-vendor: curl -L -o vendor/xlsx-0.20.3.tgz that URL
