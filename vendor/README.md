# Optional offline copy of SheetJS Community Edition 0.20.3 (patched).
# Runtime dependency is the CDN URL in package.json / overrides:
#   https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz
# Keep this tarball for air-gapped re-vendoring: npm i file:vendor/xlsx-0.20.3.tgz -w @m1/server
