version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 1
    ignore:
      - dependency-name: xlsx
      - dependency-name: kysely
      # Optional PDF helper; image sets PUPPETEER_SKIP_DOWNLOAD. Majors break lockfile sync
      # (Dependabot writes it into dependencies instead of optionalDependencies).
      - dependency-name: puppeteer
    groups:
      all-npm:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
        exclude-patterns:
          - "kysely"
          - "xlsx"

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 1
    groups:
      all-actions:
        patterns:
          - "*"
