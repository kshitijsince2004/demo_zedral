# Copy to deploy/.env on the EC2 VM (never commit deploy/.env)
# Generate secrets: openssl rand -hex 32

# ─── HTTP ────────────────────────────────────────────────────────────────────
HTTP_PORT=80

# ─── PostgreSQL ──────────────────────────────────────────────────────────────
DB_USER=m1_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_NAME=m1_db

# Used by node-pg-migrate in container entrypoint (optional if DB_* set)
DATABASE_URL=postgres://m1_user:CHANGE_ME_STRONG_PASSWORD@db:5432/m1_db

# ─── Auth (required in production) ─────────────────────────────────────────────
JWT_SECRET=CHANGE_ME_AT_LEAST_32_CHARS_RANDOM
AUTH_STRICT=true
VALIDATION_STRICT=true

# Single-tenant pilot — must match seeded tenant UUID in database
TENANT_ID=00000000-0000-0000-0000-000000000001

# Comma-separated allowed browser origins (web + operator APK).
# Production example: https://hsl.zedral.com,https://localhost,capacitor://localhost
# Leave empty for same-origin only when SPA and API share one host.
CORS_ORIGIN=

# ─── Runtime flags ───────────────────────────────────────────────────────────
RUN_MIGRATIONS=true
EXPORT_WORKER_ENABLED=true
EXPORT_WORKER_POLL_MS=5000
DPR_SCHEDULER_ENABLED=true
DPR_SCHEDULE_HOUR=7
SHIFT_BOUNDARY_SCHEDULER_ENABLED=false

# ─── Optional object storage for export artifacts ────────────────────────────
MINIO_ENDPOINT=
MINIO_BUCKET=m1-exports
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_REGION=us-east-1

# ─── Optional Elasticsearch (traceability search) ────────────────────────────
# Leave ELASTICSEARCH_URL empty to use PostgreSQL-only traceability fallback.
ELASTICSEARCH_URL=
ELASTICSEARCH_API_KEY=
ELASTICSEARCH_USERNAME=
ELASTICSEARCH_PASSWORD=
