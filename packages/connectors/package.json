{
  "name": "@zedral/connectors",
  "version": "1.0.0",
  "description": "Zedral edge connector framework for M1 / Manifold integrations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc -b ../platform --force && tsc --noEmit",
    "start": "node dist/runner.js"
  },
  "dependencies": {
    "@zedral/platform": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
