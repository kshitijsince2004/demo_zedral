# Offline copy of SheetJS CE 0.20.3 (patched).
# Runtime dep + overrides use the vendored tarball (Dependabot-safe):
#   "xlsx": "file:vendor/xlsx-0.20.3.tgz"
# Re-vendor: curl -L -o vendor/xlsx-0.20.3.tgz https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz
