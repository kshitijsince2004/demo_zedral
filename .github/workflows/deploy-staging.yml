name: Deploy Staging (retired)

# RETIRED — superseded by deploy-aws.yml (AWS QA after CI).
# Kept so old badges / bookmarks do not 404. Do not add deploy logic here.

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Point operators to deploy-aws.yml
        run: |
          echo "::error::deploy-staging.yml is retired."
          echo "Use workflow 'Deploy AWS QA' (deploy-aws.yml) — it runs automatically after CI on main,"
          echo "or trigger it manually with an image SHA / latest-main."
          exit 1
